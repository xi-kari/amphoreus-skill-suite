# 翻译器整改批次独立核查报告

- 审核方：fable5（事后独立审核方；只审不改，新增写入仅限交付包 `审核\` 子目录）
- 审核日期：2026-09-02
- 被审对象：codex 按《执行提示词_codex.md》执行的翻译器整改批次。实际推进：步0 → 批次A步骤1（合同级部署）→ 批次A步骤2（行为验证）`FAIL_STOP`；批次B、批次C、步骤5 未进入，批次D 未授权。
- 规范基准：`D1_修法矩阵.md`／`D2_评测方案.md` ＞ `执行提示词_codex.md` ＞ 交付包自述；审核执行《审核提示词_fable5.md》七域全跑。设计稿五件 SHA256 与 `00_基线\design_sha256_改前.json` 登记全等（审核提示词 6C61CEAF…、执行提示词 92DFBA6B…），审核窗内零改动。
- 方法：机器可算项全量复算、审核方自写脚本（零复用执行方脚本）；行为评分自组 11 员隔离盲评面板重评；KB 引文逐锚实检（`doctor` status=ok、quick_check=ok 后进行）；ROLLBACK 沙箱亲跑＋防呆测试。全部脚本与输出入册（§八索引）。
- 半途停止件口径（审核提示词 §G）：只审已完成批＋停止报告三要素；未执行批次核「确实未执行＋披露如实」，不倒扣。

## 一、总裁定

**文书级退回**（卡 SHA 不变；完成 §四 文书/脚本补正与 §七 遗留清单增补后，已完成部分可交用户勾选）。

要点：
1. 批次A 行为门维持执行方自判 `FAIL_STOP`；停止程序合规（硬规7/8 与止损条款），停止报告三要素（完成物/未完成物/恢复点）齐备且与在册证据一致。
2. 遗留项 L-01（HYSILENS-04 冻结题目缺陷）裁定经对抗复核**维持**，并新增双批判卷分歧实证（§六）；处置权在用户。
3. MAJOR 两项：F-1（ROLLBACK.ps1 在生产主机不可按文档运行——文书级可修）；B-1（批次A 部署的阿格莱雅降级句式句内含防火墙词「逐字」——修复须动卡 SHA，列入恢复执行首个卡级批次必修项，不改变本批停摆状态）。
4. MINOR 四项文书补正；遗留清单增补 6 条——按审核提示词 §F「有缺口未列入＝文书级退回」，总裁定级别由此锁定。
5. 无 BLOCKING、无作废事由：硬规 1–10 全量机器扫零违反；生产可恢复性双通道证实（baseline 快照 44/44 有效；执行方两组在册复原树经本审逐字节比对与 baseline 全等）。

## 二、分批裁定

| 批次 | 裁定 | 依据摘要 |
|---|---|---|
| 步0 基线与冒烟 | 通过 | 三方 44 文件基线全量复算成立（改前指纹 4 棵×44 条 SHA＋字节数全对，bytes_per_tree=412,609）；mydei 冒烟 PASS 且宿主段标/评分泄漏双零可复核；CLI 401 失败原样入册（JSONL SHA 吻合） |
| 批次A 步骤1 合同级 | 通过（携 B-1 MAJOR、B-2 MINOR 两处合同缺陷，复批必修） | 两树字节全等、改动恰为申报 4 文件、diff 与 D1 条款逐项对照一致、validate +33/−0 纯加严、三处 `--wave all` 亲跑 exit 0 |
| 批次A 步骤2 行为 | FAIL_STOP 维持；已完成证据可信 | 65/65 transcript 齐档、隔离扫描零命中、抽样重评完成（分歧两处且方向均为在册更严，见 §五） |
| 批次B 卡级试点 | 未执行（如实） | 生产树零「内核区」实扫为证；mydei/march7th persona 与 baseline 逐字节相等 |
| 批次C 卡级铺开 | 未执行（如实） | 同上 |
| 步骤5 全量专项 | 未执行（如实）；已备件全部独立复算成立 | T3 自测独立复扫一致；T5 三场预核声明=推导全等 |
| 批次D 阈值化 | 未授权未执行（如实） | — |

## 三、分域结论

### A 部署与指纹 —— 全绿
- 两树互比：claude/codex 各 44 文件逐文件 SHA256 全等（差异 0）；与 baseline 差异恰为申报 4 文件（`amphoreus/references/common.md`、`amphoreus/references/relations.md`、`amphoreus-aglaea/SKILL.md`、`amphoreus/scripts/validate.py`）；`modified\skills` 44 文件与两树全等（`modified\` 第 45 件为 manifest.json 快照文书，非生产件）。
- 改前段自洽：`prod_sha256_改前.json` 四棵（codex/claude/repo/baseline）×44 条 SHA 与字节数全量复算全等（超出「抽 10」底线）；`frozen_sha256_改前.json` 16 文件全等。改后段：`deploy_批次A.json` before=baseline、after=两树现值、expected=after 全量吻合。repo 链条：worktree 干净＋HEAD 374c1229…（2026-09-01 09:15:57+08:00，早于执行窗 2026-09-02 04:20）＋改前记录 repo=baseline → repo 全程未动成立。
- validate 亲跑：claude、codex、baseline 三处 `validate.py --wave all` 审核方自跑均 exit 0，字面输出含 `behavior=not_run_by_static_validator`。
- validate diff 复算：validate.py +33/−0 纯加严（圆桌合同断言＋防火墙运行时解析器，零删除零放宽）；common +21/−3（−3 为 D1 指定的 L71/L72 两句显式改写与一空行）；relations +5/−0；aglaea +1/−1 等行改写（83 行不变）。D1 批次A 全部条款（①-a 深度门行＋圆桌小节九条、①-b 三条、①-c 三条、①-d 三条、主持第五件事半句、L71/L72 消双解）在部署 diff 中逐项在场。
- T3 禁词单源：validate 与 `圆桌专项\t3_scan.py` 均运行时解析 common 条款文本，扫描侧无自带副本；词表副本仅存于 `workdir\apply_batch_a.py`（批次A 写入脚本的编辑载荷，写侧来源，非扫描副本）。
- 复合指纹：`all44_manifest_sha256=20CA228A…`（VERIFICATION.txt L32）经 54 种拼法未复现（→ F-2 MINOR）；底层 44 文件逐哈希已全部独立复核，指纹仅失去独立可复算性，不影响任何实质结论。

### B 硬规合规 —— 机器扫全绿；合同内容缺陷两处
- anaxa 锁死：`amphoreus-anaxa` 全部文件两树 vs baseline 逐字节相等。评测冻结：evals 14 文件（rubric＋13 题卡）逐字节相等。persona 13 卡零变更；两树全文零「内核区」字样（与未进批次B 自洽）。
- 编辑卫生：两树 CRLF=0、行首 `\d+\t` 污染=0；U+00B7 逐文件计数与 baseline 全等（41 处均为基线既有，零新增 `•`→`·` 替换）；U+2022 语音组名无损。
- 阿格莱雅 SKILL.md：83 行不变；「库里未检索到」已移除；**B-1（MAJOR）**——L36 登记可说出句式「织机上没有这一缕——现有卷轴没有留下它，我不会用空线补上；你若追问，我把标准字样逐字还原。」句内含防火墙 20 词表之「逐字」（common.md L47 禁其入任何场景角色台词；T3 豁免窗＝紧邻用户追问轮，不覆盖主动说出的降级句式）→ 该句式一经在陪聊/圆桌场说出即 T3 台词区命中，合同自冲突。审核方自写扫描证实全家族 26 个卡文件中这是唯一「引号内可说出跨度」命中。句为执行方自拟（D1 只要求去「库里未检索到」，未给成稿）。
- **B-2（MINOR）**——common.md L49 防火墙第三条尾句「阿格莱雅现行降级句式句内裸露『库里未检索到』即本条整改对象…」系 D1 草稿原文照抄，同批部署后即失真（现行句式已无该字样）；词表在合同文本区合法、无扫描影响，属合同内部自述过时，缺陷源自 D1 草稿未作时效中立表述。
- 越界扫描：设计稿 5 件 SHA 与改前登记全等；`D:\研究\全体会议\` 全部文件 mtime 早于执行窗，零触碰。

### C 引文与内核区保真 —— 实存锚 11 处逐字实检全命中；内核区不存在
- 批次B/C 未执行 → 全家族无内核区，「其余八卡每卡抽 ≥2 锚」无对象，记未执行而非未审；批次A 行为证据内零 KB 直引（cerydra 转录 5 处「原句」字样为题面格式词「用户输入原句：」，非引文）。
- 必检四卡指名锚（KB 实检）全部命中：
  - 白厄「命运，足以超越神谕。」＝pageid=19602/revid=73624 星魂激活，逐字（content_sha256 F3E2B539…）。
  - 海瑟音自缚＝20079/77803 行 357（登记区间 312–359 内），speaker=**海瑟音的记忆**、utterance 级 assertion_mode=**explicit** —— D1/D2/执行提示词的批判员勘正口径（限定由 speaker＋场景承载，不写 hypothesis 单字段）与库中元数据完全一致，防回潮条款落实，零违反。
  - 海瑟音颓美翼「纵然明天虚假，却依旧醉人。」＝20075/95652 见闻，原句连续尾段，逐字。
  - 三月七「编背景故事」＝2551/77408 行 160，分支「真是意外。」canon_scope=optional_branch，逐字且分支身份登记齐。
- D1 样例内核区（批次B 成稿）锚超额实检 7 处：万敌 M-V05／M-V18（去首句，连续子串）／M-D04（去尾段，连续子串，16676/77765 行 219）／M-V20（均 18022/95700 名下）、三月七 M-V08／M-V04（405/53499，存护/巡猎两形态同文）、E-V11（长夜月 24015/99158「关于缇宝」，voice_id=2b9f0bce…、content_sha256=4DB90B50… 与 persona 登记表行完全一致）。截段两处 D1 已带批判员截断注记，与 validate 引文冻结口径的对齐处置留待批次B（L-06 覆盖）。
- 其余 D1 引用键（M-V01/M-V02/M-V09/M-V15/M-V19/M-V10/ME-D01/ME-D05/ME-D07/E-V07/M-A01）为 baseline persona 既有登记行（带 voice_id＋content_sha256，波1–4 已审），本批零字节变更（域B persona 冻结复核覆盖），未重复实检。

### D 行为证据 —— 齐档如实、隔离成立、抽样重评完成；两处判卷分歧（均为在册更严）
- 齐档核：65 题 transcript 13/13 卡在册且 SHA 全对（summary.json 声明值全量复算吻合）；canonical scores/cross 仅 hyacine 闭合（5/5 PASS＋交叉 5/5 agree，SHA 吻合）；aglaea/anaxa 各有首评＋受影响题 rerun 三件套（SHA 吻合），canonical 合并因停摆未完成（L-02 如实）；hysilens 首评＋rerun1＋fresh cross 齐（SHA 吻合）。失败轨迹 7 文件＋定位文书 SHA 全对；无效身份评分件隔离在册不参与裁决（E-07/E-15：同身份交叉在产物生成前被中断、另起全新身份）。
- 冻结输入：65 题输入逐字在场 60/65；hyacine canonical 转录缺「用户输入」回显段（→ D-1 MINOR；输入可由冻结题卡＋`workdir\blind_inputs\hyacine.json` 回溯，后者纯净已验）。cerydra/cipher 用行内「用户输入原句：」格式、输入逐字在场。cyrene 转录标题标准化经字节级重建验证＝纯标题改名、响应零改动、原件保留（E-06 处置属实）。
- 隔离验证：全扫 19 份行为文件——codex 模板指纹（PROCESS_RECORD／START:／RESULT:／SENSATION／实际完成数／缺答数／当前：对象）命中 **0**；评分标准字样在表演正文命中 **0**（11 处词面命中全部位于运行器证据头的 rubric_path/rubric_sha256/rubric_body_read=false 登记行，另 1 处为「输入输出契约」日常语义误匹配）。13 份盲投喂文件零期望/禁止泄漏；`workdir\step2_manifest.json` 13 卡 SKILL/persona SHA 与当前部署树全量一致——转录头登记的 .codex 路径与 .claude 树字节全等（域A），补足盲评面板（只读白名单、无哈希工具）声明的哈希核验缺口。
- 抽样重评（11 员隔离盲评面板：aglaea/hyacine/hysilens 三卡全量 15 题＋aglaea-05/hysilens-04/anaxa-02 三份 rerun＋mydei-05/march7th-02/phainon-04/cipher-05/terrae-01 跨卡散抽 5 题；每员仅授权 Read 白名单＝rubric＋题卡＋被评转录＋部署卡文＋common，禁读一切评分/交叉/文书）：
  - hyacine 全卡：审核 5×PASS ＝ 在册 canonical 5/5 PASS，结论一致（分值层 Q4 在册 10/12 vs 审核 12/12，同为 PASS）。
  - anaxa-02 rerun、aglaea-05 rerun：审核 PASS ＝ 在册 rerun＋交叉 PASS，一致。
  - 散抽 5 题：审核全 PASS；停摆前无 canonical 在册对照，记独立验证通过（未闭合卡内无隐藏灾难）。
  - **分歧1**：AGLAEA-05 首跑——在册 10/12 FAIL（F2/F5=1），审核盲评 12/12 PASS。方向＝在册更严；执行方已按 rubric「单题失败→重跑受影响题」处置，rerun 经其盲评＋交叉与审核盲评三方一致 PASS，差 canonical 合并（L-02）。
  - **分歧2**：HYSILENS-04——在册首跑 9/12 FAIL、rerun 8/12 FAIL＋交叉维持；审核盲评对两份转录均 12/12 PASS。方向＝在册更严；即停摆题本体（§六）。
- 批次B 增益裁定全量复核：批次B 未执行、无 mydei/march7th 新旧双答，依半途停止件记不适用。

### E 圆桌专项复算 —— 已备件全部独立复算成立；五件均未运行（如实）
- T3：本批扫描 NOT_RUN（门停）与 L-04 一致。已备件复核：`t3_scan.py` 运行时解析 20 词（审核方自写解析同得 20 词、无重复）；对执行方自测标的（群聊批 shift-transcript）审核方自写分类器独立复扫＝台词 0／台账 4／用户 0，与执行方自测完全一致；块归属争议 0 条。
- T4／T1／T2：未运行（依赖批次B/C 与圆桌转录），无可复算对象，如实。
- T5：三场题面＋预核＋manifest SHA 全对；每场 6 人、15 对逐对预核；三场「声明禁区对」与审核方按 relations 同场禁区表独立推导的交集**逐场全等**（R1：万敌×三月七、三月七×缇宝、三月七×阿格莱雅；R2：刻律德菈×赛飞儿、刻律德菈×那刻夏、缇宝×那刻夏；R3：三月七×海瑟音、三月七×缇宝、三月七×阿格莱雅）；搭桥安排与 D2 指定一致。判卷规范对照 D2：七项阈值零放宽，且含批判员修订负例（纯复述不计有效回应）与「搭桥两条边不得伪装禁区直接双向」防作弊条款。表演/双评/交叉未运行（L-05）。
- 升降档复跑：题面三轮＋判定规范七必检项与 D2「防火墙不误伤升档审计件」意图逐条对应；未运行（L-03）。
- 方法注记：审核方宽松正则从 relations 禁区节提取到 22 个对状字符串、其中 3 个为散文片段误配；实质核对以逐场交集比对为准（全等），19 对计数取自 hash 已验的预核件。

### F 文书与披露 —— 账实全符；回滚脚本 MAJOR；遗留清单须增补
- 验收单：逐批节完整（改动清单/diff 摘要/验证结果/偏离申报/失败轨迹索引 12 件全 SHA 对上）；通道偏离（Codex 环境承担＋CLI 401 后改隔离 collaboration）按预授权披露；末行「待核：交 fable5 审核，勾选权在审核方与用户」逐字在场（L99），勾选状态未勾选，零代勾。
- VERIFICATION.txt：9 项工件哈希全量重算吻合；FINAL_STATIC_CHECKS 14 项逐项由审核方独立事实复核成立。
- **F-1（MAJOR）回滚脚本不可按文档运行**：ROLLBACK.ps1 使用 .NET Core 专有 API（L30 `IsPathFullyQualified`、L38/L46 `GetRelativePath`）；生产主机 Windows PowerShell 5.1.26100.8894 无此方法（MethodNotFound 探针实录），pwsh 7 未安装（where 查无）。审核方沙箱亲跑：`powershell … ROLLBACK.ps1` exit 1、**零文件写入**（fail-closed，无半程损伤），生产四文件前后哈希零变化。ROLLBACK.sh 包装 powershell.exe → 同败。VERIFICATION [ROLLBACK] 段（L82–106）命令行系 `workdir\generate_verification.py` L74–88 手工拼装而非执行实录，与实际成功环境不符。缓解事实：执行方两组在册复原树（rollback_test_batchA、rollback_test_final 各 codex/claude）经审核方 4×44 文件逐字节比对与 baseline **全等**——恢复能力本体已证，坏的是随附一键通道及其文书。防呆：同路径互斥闸有效（先于 API 故障抛出）；缺目标闸在 5.1 下因 MethodNotFound 意外 fail-closed。VERIFICATION 的 ROLLBACK_SH_SYNTAX/EXECUTABLE 两段复核属实但仅证语法与执行位，不证可运行性——F-1 即其反例。
- **F-3（MINOR）**：`文书\圆桌与陪聊使用指南.md` 三件套齐全（三场对照表＋相容性说明＋三句话术逐字），但相容性说明为改写、未含 D1 ④「全文照抄」中的 codex 模板文件名与行段锚（gpt-5.6-sol-unrestricted-v45.md L22–37 等），削弱可回查性且未申报改写。
- 遗留清单：既列 10 条与证据一致；但对照本审 A–F 发现，B-1/B-2/F-1/F-2/F-3/D-1 六项缺口未列入 → 按 §F 条款构成文书级退回（增补建议见 §七）。

### G 对抗批判 —— 核心裁定试驳失败（维持）；停止件合规
- 原定两条受驳结论（「批次B/C 后声纹增益成立」「T5 互动真实而非编排」）本批均未作出，依半途停止件转为对本批实际核心结论的试驳。
- **试驳「HYSILENS-04 属冻结题目缺陷」**（反方立场：卡文缺陷或执行缺陷）——**驳不倒，裁定维持**：
  1. 不相容性逐字核实：冻结输入零教训正文（题卡逐字）；冻结期望 92 字与定位文书引文逐字符相等（审核方修正提取器后 92/92 全等）；卡文六要素门槛与固定移交行在 SKILL 在册；common 禁补写在册。凭空产出候选＝触发 rubric 硬失败第 4 条（虚构）。
  2. 双批五次独立判卷分裂：群聊批（2026-08-31，同卡文同题）对同质拒答行为判 12/12 PASS（该批评分 notes 已留痕「题卡张力…唯一正确处置」）；本批两员判 FAIL（9/12、8/12＋交叉维持）；审核面板两员判 PASS。3 PASS／2 FAIL 的分布证明该题在冻结判卷线下不存在稳定可判定性——题目缺陷的定义性证据；并回溯说明缺陷为潜伏既有（上一批 65/65 全绿部分依赖判卷员的宽容读法），非本批引入。
  3. 两轮同因可重复（两个全新隔离表演体同停于同一材料缺口），排除偶发声线漂移；执行方不改卡、不改题、不现场校准而停摆，恰为硬规7 的合规处置。
- **试驳「65 份转录出自隔离表演而非执行方代笔」**——未获反证：失败轨迹纹理（0/5 空产出、ParserError、无效身份评分、401 JSONL）非代笔者会自造；转录头格式跨卡异质（YAML/圆点两式）符合逐次装配；审核盲评 30 题逐字证据级复核全部与卡文合同细粒度吻合；宿主模板指纹零命中。方法学限界如实声明：产物层证据可证「内容与隔离登记自洽」，不能绝对证明子代理来源，本审以在册失败证据＋盲评一致性为充分佐证。
- 半途停止件：停止报告三要素齐备（验收单已完成/未完成节、定位文书恢复点、遗留清单当前恢复点），与证据零冲突；未执行批次未倒扣。

## 四、Findings 清单（BLOCKING 0／MAJOR 2／MINOR 4）

| 编号 | 级别 | 域 | 事实 | 证据 | 最短修法 |
|---|---|---|---|---|---|
| B-1 | MAJOR | B | 批次A 部署的阿格莱雅登记降级句式句内含防火墙词「逐字」，合同自冲突（一经说出即 T3 台词区命中，追问豁免窗不覆盖主动说出） | 两树 `amphoreus-aglaea\SKILL.md` L36；`common.md` L47 词表；`审核\outputs\C_supplement_audit.json` card_firewall_scan.quoted_speakable（全家族唯一命中） | 恢复执行首个卡级批次改写句尾（例：「你若追问，我把标准字样一字不差还原」），依 SHA 账补该卡五题＋交叉；或经用户裁量修订 D1 词表（触发 65 全量）。二择一，与 L-01 复批同车 |
| F-1 | MAJOR | F | ROLLBACK.ps1 依赖 .NET Core 专有 API，生产主机 PowerShell 5.1 报 MethodNotFound、pwsh 未装；ROLLBACK.sh 同败；VERIFICATION [ROLLBACK] 命令行系拼装非实录。fail-closed 零损伤；执行方在册复原树真实（4×44 逐字节全等） | `ROLLBACK.ps1` L30/L38/L46；`审核\outputs\F_rollback_audit.json`、`F_rollback_pwsh_audit.json`（5.1 版本与方法探针、exit 1 stderr 实录、沙箱前后比对、生产零触碰）；`workdir\generate_verification.py` L74–88；`VERIFICATION.txt` L82–106 | 改写两处 API 为 5.1 兼容（IsPathRooted＋盘符/UNC 前缀校验；FullName 前缀截取替代 GetRelativePath），在生产主机以 `powershell` 实录沙箱干跑 exit 0，重生成 VERIFICATION [ROLLBACK] 段并注明解释器版本 |
| B-2 | MINOR | B | common.md 防火墙第三条尾句时效失真（部署同批即过时；D1 草稿原文照抄所致） | `common.md` L49；`diff\批次A.diff` | 下次 common 变更搭车改历史注记或删除（65 全量搭车零边际），并在验收单申报 |
| D-1 | MINOR | D | hyacine canonical 转录缺「用户输入」回显段（其余 12 卡均有），冻结输入不在场文件内 | `行为重跑\hyacine-transcript.md`（首标题即响应）；`审核\outputs\F_rollback_audit.json` hyacine_inputs；`B_integrity_audit.json`/`C_supplement_audit.json` 逐字回对两节 | 文书级补一份带输入回显的重排版（响应字节零改动、原件保留）或在 summary.json 注记格式偏差与输入回溯路径 |
| F-2 | MINOR | A/F | `all44_manifest_sha256=20CA228A…` 复合指纹公式未文档化，54 种拼法未复现，独立可复算性缺失（底层 44 文件逐哈希已全对） | `VERIFICATION.txt` L32；`审核\outputs\A_tree_audit.json`、`B_integrity_audit.json`、`F_rollback_pwsh_audit.json` composite 三节 | 在 VERIFICATION 注明生成公式，或改为可复算定义（如「44 行 `path:sha256` 升序 LF 拼接的 SHA256」）并给出新值 |
| F-3 | MINOR | F | 指南相容性说明为改写、缺 D1 ④ 模板来源锚（文件名/行段），未申报 | `文书\圆桌与陪聊使用指南.md`；D1 ④；`审核\outputs\C_supplement_audit.json` guide_components | 补一行来源锚（模板文件名＋行段）或在验收单申报「按语义物化非逐字」 |

## 五、抽样一致性条款的触发与吸收（如实披露）

审核提示词 D 域规定「单处不一致→该卡全量重查，第二卡不一致→行为级退回」。本审两卡各一处 PASS/FAIL 不一致（AGLAEA-05 首跑、HYSILENS-04），条款字面触发；两卡全量重查已在面板内完成。但两处分歧方向均为**在册更严**（条款所防的是在册过宽放行），且两题均已在执行方自身流程内闭环：AGLAEA-05 已按 rubric 重跑并三方一致 PASS（差 canonical 合并＝L-02）；HYSILENS-04 即 FAIL_STOP 本体（L-01）。机械执行「行为级退回＝指定题目重跑」将命令重跑两道已完成重跑/已停摆待用户裁定的题，且第三次重跑不能改变冻结输入与期望的不相容。故裁定：条款触发记录在案，其处置**并入 L-01/L-02 既有恢复点，不新增重跑题目**；总裁定按文书级退回执行。若用户要求机械执行行为级退回，其对象即 L-01/L-02，无额外工作项。

## 六、L-01 复核结论与用户决策项

裁定维持：HYSILENS-04 属冻结题目缺陷（证据 §G）。依冻结纪律，rubric 与 65 题卡不可现场校准；恢复路径二选一，均须用户显式授权：
1. 授权修订该冻结题（最小改法任一：输入补一条含事实样本与证据的复盘教训；或期望改写为「保留事实包结构与候选证据门槛，等待材料」），另开新批从 L-02 恢复点续跑（canonical 闭合→升降档→T3/T5 首跑→批次门复判→批次B/C）。
2. 判本轮止步：决定批次A 四文件改动去留——保留（接受 B-1/B-2 于后续批修复）或回滚至 baseline（建议先完成 F-1 修复以恢复一键通道；亦可用已验证的 baseline 快照按 44 文件逐哈希手工恢复）。

## 七、遗留清单增补建议（续 L-10 之后）

| 编号 | 状态 | 遗留项 | 恢复条件 |
|---|---|---|---|
| L-11 | 缺陷（MAJOR） | B-1：阿格莱雅 SKILL.md L36 降级句式含防火墙词「逐字」 | 与 L-01 复批同车：改写句式＋该卡五题＋交叉（SHA 账既定成本） |
| L-12 | 缺陷（MINOR） | B-2：common.md L49 时效句失真 | 下次 common 变更搭车修正并申报 |
| L-13 | 缺陷（MAJOR） | F-1：ROLLBACK.ps1/AsShipped 在 PowerShell 5.1 不可运行；VERIFICATION [ROLLBACK] 段非实录 | 文书级补正（无需新批授权）：5.1 兼容化＋生产主机实录干跑＋重生成该段 |
| L-14 | 文书（MINOR） | D-1：hyacine canonical 转录缺输入回显 | 文书级重排版或 summary 注记 |
| L-15 | 文书（MINOR） | F-2：复合指纹公式未文档化 | 文书级：注明公式或改可复算定义 |
| L-16 | 文书（MINOR） | F-3：指南缺 D1 ④ 来源锚且改写未申报 | 文书级：补锚或申报 |

## 八、复算证据索引

- 审核方产物全部落 `翻译器整改交付\审核\`：`scripts\`＝复算与取证脚本（含盲评面板工作流脚本归档），`outputs\`＝复算输出 JSON 与盲评面板原始结果；`outputs\rollback_sandbox*` 为回滚测试树（不入清单，终态见 F_rollback*.json）。
- 盲评面板：11 员并行隔离子代理（Fable 5），逐员只读白名单，结构化输出强制 schema；原始返回全文＝`outputs\D_blind_rescore_panel.output.json`。
- KB 实检：`starrail_knowledge_base` MCP，先 `doctor`（status=ok、quick_check=ok、FTS/双向量计数一致），锚点证据（pageid/revid/行号/content_sha256）已写入 §C。

| 文件 | 字节 | SHA256 |
|---|---|---|
| scripts\audit_a_tree.py | 16961 | C3520D4200423CD354580DE9685D1B069B54CEF29BBAB1394A80421BD81AAC70 |
| scripts\audit_b_integrity.py | 14436 | F6D66951FFE705F730BE8702519071183A6FC09E2A2C6F64C2C27759B16EC991 |
| scripts\audit_c_supplement.py | 11070 | 1FC972D5E5477311F2FD1DB71EEBCF2553E19D7974BE019372CDEE4727FFF38F |
| scripts\audit_f_rollback.py | 7103 | 37250A0F265B701D0833AD6A24B499230955AF797945883FDFFF47E422D7040D |
| scripts\audit_f_rollback_pwsh.py | 8480 | A56A80ECF297B577B046A98D45047AC7A82480C67A8280B81D5EB1638ECA197D |
| scripts\audit_g_final.py | 3876 | E4972A4AFDE1712153C78B28C2FA1AD71757C50642FA17E6D6A6335E55E615DA |
| scripts\blind_rescore_workflow_wf_238cc4a8.js | 5027 | 7DEDFB678B270F0C2CE4EC79457C1CEE27AF7D44201F8176BAD3F992FAD524AE |
| scripts\write_report.py | 26747 | 8EBCAC76AE9CEEACE11A887B6ACF6B6FAFE735D7C1B90941B1942960083934BD |
| outputs\A_tree_audit.json | 47802 | 43CCA0D471CAE930C0EB53366D5DF8F0905596546E4226F9FD63D3B4B52486D9 |
| outputs\B_integrity_audit.json | 18623 | F48A5B33DD3613A8928A29F9D7735A66652AC3E4460CBD352136ACF51A0FAAC4 |
| outputs\C_supplement_audit.json | 7067 | 364865CFF0F50B97EE57C8FB9069F502A8A72D9799D117D8ADF201207BA8F7E4 |
| outputs\D_blind_rescore_panel.output.json | 67251 | 91800D6D7D1DCEADF6AE03F04131120534C358F3269FA3289257C872B2F9A24B |
| outputs\F_rollback_audit.json | 3134 | D12858600BC9CE42EEDF1DD1D67C60EB4717EBC4F639436FE52013C5CE293553 |
| outputs\F_rollback_pwsh_audit.json | 3432 | 0122397837DDFC2C99245B8DA14EF05E4CE266D472791D1A8007AB1370793172 |
| outputs\G_final_checks.json | 2966 | F610BA0077BCB0E87DF1587A2C086AD6489678CA34CB7CE44F21BBA7808955FB |

## 九、2026-09-02 补正追记（用户委托「你帮我决策、报告有问题的直接改完」后执行）

§四 文书/脚本补正与 §七 遗留清单增补已全部落实；本节为执行记录与两项决策的依据。本次补正零生产树变更、零 evals/rubric 变更、零卡 SHA 变更。

### 已闭合项

1. **F-1（MAJOR）闭合**：`ROLLBACK.ps1` 重写为 Windows PowerShell 5.1 兼容——正则绝对路径闸（盘符/UNC 前缀）替代 `IsPathFullyQualified`，`FullName` 前缀截取替代 `GetRelativePath`；另修两处顺带缺陷：补 UTF-8 BOM（5.1 读无 BOM UTF-8 会按 ANSI 代码页错解中文注释与 `D:\研究\…` 基线路径字面量，原脚本同病、只是先死于 MethodNotFound 未走到）；缺目标闸从逐树循环内提前为复制前全目标预检（原逻辑第一棵树复制完才发现第二棵缺失，非真 fail-closed——即 §F 所指「意外 fail-closed」的根治）。生产主机 5.1.26100.8894 实录六项测试全过：正跑双沙箱树 44/44 恢复至 baseline 并 validate exit 0、缺目标零写入、同路径互斥、相对路径拒绝、`ROLLBACK.sh`（WSL 通道，裸 `bash` 经 CreateProcess 落 System32 WSL bash，脚本路径须 /mnt/d 视角）恢复成功、生产四文件前后哈希零变化。驱动 `workdir\test_rollback_ps51.py`（SHA256 075A0CCB…），原始实录 `workdir\rollback_ps51_实录.json`（6437B7DB…）；新脚本 SHA256 20EBF65B…（3532B），原件存档 `workdir\ROLLBACK_预整改_FFE2DA4F.ps1`；VERIFICATION [ROLLBACK] 段已重生成为实录并注明解释器版本，新增 [ROLLBACK_GUARDS]/[ROLLBACK_SH_RUN] 两段。
2. **D-1（MINOR）闭合**：新增 `行为重跑\hyacine-transcript-重排版.md`（837D18ED…，15102B），由 canonical 原件＋冻结盲测输入机械合成（仅插「### 用户输入/### 用户可见响应」段与一条说明），响应字节零改动、原件保留；合成脚本 `workdir\build_hyacine_reformat.py` 内置自检＝剥除插入段后与原件逐字节相等，已通过。
3. **F-2（MINOR）闭合**：VERIFICATION 增记可复算定义 v2（44 行 `relpath:SHA256大写` 按码点升序、单 LF 拼接无结尾换行、取 UTF-8 字节串 SHA256），新值 `all44_manifest_v2_sha256=AB156A94…`，旧值降级为历史留档；脚本 `workdir\compute_manifest_v2.py`。
4. **F-3（MINOR）闭合**：指南「Codex 相容性」节补 D1 ④ 来源锚（`gpt-5.6-sol-unrestricted-v45.md` L22–37/L49–51/L54–56/L74/L76–78＋`config.toml` verbosity）并显式申报「按语义物化、非逐字照抄」。
5. **§七增补落实**：遗留清单增 L-11–L-16；L-13/L-14/L-15/L-16 随本节闭合，L-11（B-1）/L-12（B-2）立项待复批（均动卡 SHA 或须搭车，维持本报告口径）。

### 两项决策（受托代作，执行仍待用户指令）

- **L-01 取路径 1**：按最小改法修订冻结题 HYSILENS-04（输入补一条含事实样本与证据的复盘教训），另开新批从 L-02 恢复点续跑，并与 L-11 同车。依据：双批五次判卷 3 PASS/2 FAIL 分裂已证该题冻结判卷线下无稳定可判定性，判止步等于让一道坏题冻结整条整改线并弃掉批次B/C 已备成稿；改期望（弱化为「等待材料」）会削平该题对移交门槛的测试价值，补材料保留全部考点。本次未动 evals——题面实际改动属新批授权范围。
- **批次A 四文件保留、不回滚**：validate 三处亲跑 exit 0、+33/−0 纯加严、D1 条款逐项在场，回滚将弃掉已验证的合同收益且必然再付一次 65 全量部署成本；B-1/B-2 按 L-11/L-12 于复批修复。一键回滚通道已实录可用，保留决策可随时逆转。

### 状态更新

「文书级退回」的两项完成条件（§四补正、§七增补）已满足——按 §一 口径，已完成部分具备交用户勾选条件。勾选权仍在用户，本次未代勾。

---
勾选权在用户；未获显式代勾委托时停在裁定。
