# 翻译器整改验收单

- 执行范围：`D1 批次表步0→批次C`，以批次门和止损条款为执行边界；批次D未执行。
- 生产树：`C:\Users\cangm\.claude\skills`、`C:\Users\cangm\.codex\skills`。
- 新增写入区：`D:\研究\δ-me13skill套件计划\翻译器整改交付`。
- 当前状态：三期步骤7批次B试点门 `FAIL_STOP`；批次A已关门，批次C与最终 T1–T5 未进入。
- 勾选状态：未勾选；审核与勾选权保留给 fable5 与用户。

## 步0：基线与冒烟

### 完成物

- 知识库 `doctor`：`status=ok`；SQLite `quick_check=ok`；FTS、字符向量、语义向量记录数与偏移检查全部一致。
- 改前三方基线：Claude、Codex、repo 三棵 `skills` 树各 14 个目录、44 个文件、412,609 字节，逐路径 SHA256 一致。
- 改前复合指纹：`20CA228A346E48F8BC34BC001B819BE68FF3A2357408AF33526D45DF6F68CE53`。
- `baseline\skills`：44/44 文件复制并逐文件复核一致。
- 静态基线：两棵生产树与 baseline 均执行 `validate.py --wave all`，退出状态均为 0；字面输出包含 `behavior=not_run_by_static_validator`。
- 隔离行为冒烟：mydei 当前卡文＋一道冻结输入；宿主段标命中 0、评分标准泄漏命中 0、全文取回成功，结论 PASS。
- Codex CLI 候选通道首跑因 `401 Unauthorized` 退出 1，完整事件已入失败轨迹；该输出未计入行为样本。之后使用 `fork_turns=none` 的隔离 collaboration 通道。
- 环境级指南已物化于 `文书\圆桌与陪聊使用指南.md`，repo README/docs 未改。

## 批次A步骤1：合同级修改

### 改动清单

| 文件 | 改动 |
|---|---|
| `amphoreus/references/common.md` | 深度门新增圆桌场；新增圆桌合同、工艺词防火墙、台词/台账分离；消解场级回执、出处与主持职责关系 |
| `amphoreus/references/relations.md` | 新增圆桌参数：回应对提示、禁区搭桥、素材边界 |
| `amphoreus-aglaea/SKILL.md` | 等行改写降级句式，移除台词内裸露审计短语；83 行保持不变 |
| `amphoreus/scripts/validate.py` | 纯加严：检查圆桌合同、圆桌参数并从 common 条款运行时解析 20 个防火墙词 |

### 部署与静态验证

- 临时候选 `validate.py --wave all`：PASS，退出 0。
- 双树部署后逐文件复核一致；当前改动文件数 4，其余 40 文件与 baseline 一致。
- 双树改后 `validate.py --wave all`：PASS，退出 0。
- 全树 LF：是；行首 `\d+\t` 污染：0；阿格莱雅 `SKILL.md`：83 行。
- `amphoreus-anaxa` 两文件零变化；`amphoreus/evals` 14 文件零变化。
- repo 分支 `main`、HEAD `374c12290dc766ebb556b6fbcade1a4e535d004b`、工作树干净，未提交、未推送。

### 偏离申报

- D2 写有默认 Claude 侧通道；本批按用户当前指令由 Codex 环境承担，并在 CLI 401 后改用隔离 collaboration 子代理通道。
- D1 建议 repo docs/README 收录；本批硬规要求 repo 零触碰，因此指南仅进入交付包文书，repo 收录保留为遗留项。

## 批次A步骤2：行为验证与停止

### 已完成

- 表演层：13 卡 × 5 冻结题，原样 transcript 共 65/65 题全部落盘；每卡独立表演体，未向表演体提供 rubric、期望、禁止项或评分材料。
- HYSILENS-04 首轮：9/12，FAIL，硬失败 0。
- HYSILENS-04 复跑一：8/12，FAIL，硬失败 0。
- HYSILENS-04 复跑一交叉：`agree=true`，`suggested_pass=false`，维持 FAIL。
- 两轮同因定位：冻结输入没有给出教训正文或证据，冻结期望却要求当轮形成证据完备的制度候选；海瑟音卡要求候选携带事实样本、反复模式、例外需求、成本线索、验收和废止触发，common 又禁止补写缺失材料。定位为冻结题目缺陷，不是卡文缺陷。
- 失败轨迹全部保留，包括转录写入 ParserError、CLI 401、空产出、评分/交叉格式失败、无效身份评分、首轮行为失败和复跑结果。

### 未完成

- 65 题只有 transcript 层达到 65/65；在同因失败触发停止时，尚未完成全部卡的 canonical scores/cross。
- 沙龙升降档：题面与判卷规范已准备，表演/评分/交叉未运行。
- T3：单源扫描脚本已建立并完成解析自测；本批圆桌/陪聊转录扫描未运行。
- T5：R1/R2/R3 题面、每场 6 人名单与 19 禁区表派生预核已准备；三场表演、双评与交叉未运行。

### 批次门

- 批次A门：`FAIL_STOP`。
- 停止依据：同一失败连续两轮，已定位题目缺陷；rubric 与 65 题卡在本批冻结，不能现场校准；卡文无须为缺材料而改成虚构候选。
- 恢复点：由 fable5 与用户核定题目缺陷；若要调整冻结输入、期望或 rubric，须另行授权新批次。当前不改 eval、不改海瑟音卡、不进入批次B。

## 批次B、批次C与步骤5

- 批次B：未进入；mydei、march7th 内核区未写入生产树，KB逐锚复核和两卡试点未启动。
- 批次C：未进入；十卡双翼检索、内核区、50题与交叉未启动。
- 步骤5：未进入；T1/T2/T4 全量、T5复跑与完整专项关账未启动。
- 批次D：未授权，未执行。

## 回滚与恢复验证

- `ROLLBACK.ps1` 使用显式 Windows 路径并校验 baseline 恰有 44 文件；两个目标根必须不同。
- 已在两棵 disposable copy 上恢复 baseline，44/44 哈希一致，两棵恢复副本 `validate.py --wave all` 均 PASS、退出 0。
- 生产树未执行回滚，当前仍保留批次A四文件修改，供审核方核查。

## 失败轨迹索引

- `失败轨迹\步2_CodexCLI_401.md`
- `行为重跑\anaxa-transcript-attempt1-failure.md`
- `行为重跑\aglaea-scores-attempt1-format-failure.json`
- `行为重跑\anaxa-scores-attempt1-format-failure.json`
- `行为重跑\hyacine-transcript-attempt1-failure.md`
- `行为重跑\hyacine-scores-attempt1-format-failure.json`
- `行为重跑\hyacine-cross-attempt1-format-failure.json`
- `行为重跑\hysilens-scores-rerun1-invalid-actor-identity.json`
- `失败轨迹\HYSILENS-04_两轮同因定位.md`
- `失败轨迹\执行故障汇总.md`
- `行为重跑\summary.json`
- `圆桌专项\批次A-summary.json`

### 二期失败轨迹与补丁索引

- `失败轨迹\二期_步0_旧verify_state_设计文件集合漂移.md`
- `失败轨迹\二期_步1_部署器清单作用域首跑失败.md`
- `失败轨迹\二期_步1_modified快照作用域污染.md`
- `失败轨迹\二期_HYSILENS_cross同身份无效.json`
- `失败轨迹\二期_关账_ROLLBACK首跑模块自动加载失败.json`
- `diff\二期_补丁批.diff`

---

## 二期执行

### 对账

- 一期 F-1、D-1、F-2、F-3：按独立核查报告终态 `D6EDF6781374BCA69A9C78260D540318BB865455C8A0CD4FAD8BEB80B3829C41` 核验在位，不重做。
- `ROLLBACK.ps1`：`20EBF65B5D64AE5E9D5BC4202388A660987B4CC08461A3C096CA3CD46E83B9C2`，3532B；预整改存档 `FFE2DA4F...` 在册。
- 批次A一期四文件保留不回滚；二期以该状态为 baseline。

### 步0

- `doctor`：`status=ok`，SQLite `quick_check=ok`，chunks/FTS/semantic_vectors=`62354/62354/62354`。
- 两树二期基线：44 文件、418857B、逐文件一致；baseline manifest v2=`3FEC209E669382B9CA807D12473AC6798498FF4FABF5E6E0C8927299A64532C8`。
- 两树 `validate.py --wave all`：PASS／exit 0；行为冒烟 PASS。
- 旧 `verify_state.py` 因设计文件集合新增二期提示词而首跑失败，已原样入册；修正核对作用域后步0 PASS。

### 步1 补丁批

- 双树部署三文件且字节一致：`amphoreus-aglaea/SKILL.md`、`amphoreus/evals/hysilens.md`、`amphoreus/references/common.md`。
- B-1/L-11：阿格莱雅两条降级句式不含防火墙20词，单句 T3 命中 0。
- B-2/L-12：common 时效句修正；圆桌新增“应答方式让位”“场景开合”两条。
- HYSILENS-04：只补输入证据材料，期望与 rubric 不变；其余64题不变。
- 校准凭证：`文书\换题清单_二期.md`；其中新旧 SHA 均为 `amphoreus/evals/hysilens.md` 整文件哈希。
- 候选与双树静态验证 PASS／exit 0；Anaxa 零变化；repo 零变化。
- 部署器首跑清单作用域失败和 modified 快照作用域污染均原样入册，修正后生产三文件部署与44件快照核验通过。

### 步2 行为门

- canonical transcript：13/13 卡、65/65题。
- canonical scores：13/13 卡；HYSILENS-04 校准后评分 10/12、PASS；海瑟音五题全部 PASS。
- canonical cross：8/13 卡；缺 `hysilens、mydei、phainon、terrae、tribbie`，在停止触发时未继续补齐。
- 评分失败：TERRAE-01、TERRAE-03。TERRAE-01 首轮与复跑一均 F1=1、F2=1、总分10、FAIL，且同因。
- 同因定位：冻结输入未提供目标仓库／宿主／项目栈，冻结期望却要求实际勘地与承重实录；定位为冻结题面契约缺陷。本批未授权调整 TERRAE-01。
- 已完成交叉还发现：AGLAEA-01、CIPHER-01、CIPHER-02 建议 FAIL；CERYDRA-05 结论仍 PASS 但原评分偏宽。停止后未另开处置轮。

### 批次门与未进入项

- 批次A门：`FAIL_STOP`。
- 停止依据：TERRAE-01 两轮同因失败；证据：`失败轨迹\二期_TERRAE-01_两轮同因定位.md`。
- 沙龙升降档、T3全场扫描、T5三场、批次B、批次C、步骤5 T1–T5：`NOT_RUN_DUE_GATE_STOP`。
- 二期圆桌凭证：`圆桌专项\二期\T3_阿格莱雅降级句式.json`、`圆桌专项\二期\批次A-summary.json`。
- 批次D：未授权，未执行。

### 回滚与恢复

- `ROLLBACK.ps1` 未重写；Windows PowerShell 5.1 终验在两棵 disposable phase2 modified copy 上恢复原始44件 baseline，双树 validate PASS、exit 0。
- 终验首跑发生 `Get-FileHash` 模块自动加载失败，原样入册；复跑在同一 5.1 进程显式导入官方 Utility 模块后 PASS。生产前后44件清单一致，仍保留二期三文件修改。

### 当前恢复点

- 生产树保留二期补丁三文件；baseline、modified、diff、失败轨迹、回滚证据在交付包内。
- 下一批须先裁定 TERRAE-01；不得覆盖既有失败与未完成三件套状态。
---

## 三期执行

### 步0

- 已读取三期对账段及一期、二期提示词；二期三文件、65 transcript、scores 13/13、cross 8/13 与 ROLLBACK.ps1 只核验不重做。
- 两树44文件、419762B，与二期审核终态一致；manifest v2=`B640B2B50D51D324077140DC765C77D95B5B56DBDF6E8479540976AB3D90B2E2`。
- doctor=`ok`、quick_check=`ok`；两树 validate PASS/exit0；新鲜行为冒烟PASS。

### 步1 文书六件

- F2-1/F2-2/F2-3/F2-4/F2-5/F2-6、C-1/C-2、CRIT-1、D3-06 已按授权修补或登记延期；生产树零变化。
- 二期验收/遗留终态锚分别为 `45672D90...`、`482E46F8...`；三件merge已补 `normalized_sha256`。

### 步2 四标记处置

- AGLAEA-01、CIPHER-02 驳回交叉维持PASS；新句式实际触发题号=AGLAEA-04。
- CERYDRA-05维持PASS并按静音档意象将有效分数记11/12。
- CIPHER-01改判10/12 FAIL，进入步骤3。

### 步3 CIPHER-01

- 新鲜表演、盲评、cross完成；CIPHER-01=12/12 PASS、CIPHER-02相邻回归=12/12 PASS；新canonical五题全PASS。

### 步4 Terrae题组

- 对象规格逐题审视；只校准TERRAE-01/T05输入，T02/T03/T04原题不动，期望/rubric不动。
- Terrae整文件SHA `D1FA256E...`→`6718F4A6...`；两树字节一致、validate PASS。
- 新canonical五题 `11/12/11/11/12` 全PASS；cross五题维持PASS，T04评分宽度有非阻断分歧。

### 步5 cross欠账

- hysilens/mydei/phainon/tribbie补齐，terrae绑定新canonical；有效cross=13/13。

### 步6 批次A关门

- 升降档7/7 PASS；T3首跑因多行details误分类FAIL，修分类器后台词区0命中PASS，首跑原样保留。
- T5 R1/R2 PASS；R3首轮因昔涟反向使用“小海兔”导致未登记昵称1而FAIL，复跑一双评＋cross PASS。
- 批次A正式关门。

### 步7 批次B与停止

- 万敌、三月七内核区逐锚核验后双树部署；两卡各五题canonical与cross全PASS，极性错误0。
- March失败链原样保留：首轮MARCH7TH-04 FAIL；rerun1 M04 PASS但M05异因FAIL；rerun2证据头缺SHA判无效；机械补元数据后M04=11/12、M05=12/12 PASS。
- 新旧双答盲判：万敌23>22；三月七21<23；合计new=44<old=45，胜题5:5。声纹增益门FAIL。
- 四期审核注（独立核查报告_三期 §一）：该门以十道工作场 eval 原题盲判，与 D1 ②-0「内核区作用于陪聊／圆桌场」及 D2「批次B 后走 T1/T2/T4 试点口径」不符；配对差 t=−0.26、符号／置换检验 p=1.0；单 judge、无 A/B、无 cross、无隔离元数据。门件不具裁决资格，FAIL_STOP 作为止损程序正当，但「三月七回退／无声纹增益」不作为事实沿用；四期按 D2 口径重建门复测。
- 批次B=`FAIL_STOP`；批次C、T1–T5最终全量均`NOT_RUN_DUE_GATE_STOP`；批次D仍未授权。
- 停止证据：`失败轨迹\三期_步骤7_批次B声纹增益门停止.md`。

### 步7 内核区相对 D1 成稿的漂移申报（四期补记）

部署内核区以 `D1_修法矩阵.md` ②-1/②-2 为成稿，实际落卡时发生三类未申报漂移，四期审核（C3-03/C3-04/B-1）核为语义等价、立场极性零变化：

1. 引文扩全：万敌 M-V05 由样例截段补全为 KB 整句（补「缇里西庇俄丝女士带来的神谕，」），`KB复核/三期/批次B_逐锚复核.json` status=exact。
2. 副锚删除：三月七缺样例的 M-V10「我不想…一个人……」底色第二锚与 ME-D07 长夜月侧同轴锚；万敌 M-D04 条缺 M-V19「可悲的循环……」尾注。双翼本身仍完备。删除未在三期申报，属遗漏；补回留待下次已授权动 persona 时搭车（遗留 L-30）。
3. 嵌套引号提升：M-D04「纷争」、ME-D01「她」两处内层引号按外「」内『』嵌套惯例转写为『』，与 KB 原文、卡内签名台词行在码点层不相等，语义零差；内核区不在 SKILL.md 允许直引的语料区内，无下游泄漏路径。`批次B_逐锚复核.json` 两条 status 四期已改为 `substring_after_nested_bracket_normalization` 并注明非码点逐字。

### 三期偏离申报

- 白名单①（terrae 题组对象规格校准）：`amphoreus/evals/terrae.md` 仅 TERRAE-01/TERRAE-05 两条输入行尾部追加对象在场／尺度／职权域声明；期望行、禁止行、题号、类型零改动；TERRAE-03 零字节未触（CRIT-4）。附带效应两项入遗留 L-29：T05 新输入含期望行的职权归属与允许动作清单（判别力由「能否识别越界」下调为「能否守住已告知边界」）；T01 新输入硬编码本机绝对路径（fixture 现存且 FIXTURE_MANIFEST 8/8 哈希吻合，他机执行即失效）。
- 白名单②（CIPHER-01 处置）：改判 10/12 FAIL 入册，单题重跑 12/12 PASS，CIPHER-02 相邻回归 12/12 PASS；cipher 卡文与题卡零改动。
- 白名单③（文书六件）：见「步1 文书六件」；零生产树变更。
- 白名单④（批次B 两 persona 内核区）：授权链＝二期提示词步3 经三期提示词步7 转引；两 persona 各纯插入一个 H2 节（12／11 内容行，≤25），既有行零改动；相对 D1 成稿漂移见上节。
- 通道：行为评测全程由执行方环境承担（一期已预授权偏离）。
- 三期新增自用工具（`workdir/三期/` 下部署器、扫描器修复、doctor 封装）均非生产树，首跑失败原样入册（见下）。

### 三期失败轨迹与补丁索引

失败轨迹（原样入册，未美化）：
- `失败轨迹\三期_步0_doctor取证写入封装两次失败.md`
- `失败轨迹\三期_步6_T3扫描器多行details误分类.md`（首跑 FAIL 原件保留；修复 diff 见下）
- `失败轨迹\三期_T3扫描器回滚首跑路径失败.txt`、`失败轨迹\三期_T3扫描器回滚复跑封装失败.md`、`失败轨迹\三期_ROLLBACK_T3_SCAN_首版.sh`
- `失败轨迹\三期_步6_T5-R3首轮未登记昵称.md`
- `失败轨迹\三期_步骤7_批次B声纹增益门停止.md`
- march7th 失败链原件：`行为重跑\三期\批次B\march7th-transcript-rerun1-attempt1-heading-failure.md`、`march7th-transcript-rerun2-canonical-attempt1-metadata-format-failure.md`、`march7th-scores-rerun1.json`／`march7th-scores-rerun2.json`（rerun2 判无效仅因头缺 card_sha256，正文与 `march7th-transcript-rerun2-canonical.md` 逐字节相同）

补丁 diff（`diff\` 下 8 件）：`三期_步1文书补丁.diff`、`三期_步2处置轮.diff`、`三期_步3_cipher.diff`、`三期_步4_5_terrae_cross.diff`、`三期_terrae题组校准.diff`、`三期_步6批次A关门.diff`、`三期_T3扫描器修复.diff`、`三期_批次B内核区.diff`；总变更 `三期_生产总变更.diff`。

逐步凭证：
- 步1：`文书\换题清单_二期.md` 补注、三件 `行为重跑\二期\批次A\*-canonical-merge.json` 的 `normalized_sha256`
- 步2：`圆桌专项\二期\处置轮.md`、`行为重跑\三期\处置轮\canonical_adjudication.json`、`文书\评分口径备忘_三期.md`
- 步3：`行为重跑\三期\step3\{cipher-transcript.md,cipher-scores.json,cipher-cross.json,blind_inputs}`
- 步4：`文书\terrae题组对象规格审视.md`、`文书\换题清单_三期.md`、`行为重跑\三期\step4\{terrae-transcript.md,terrae-scores.json,terrae-cross.json,terrae-tool-log.json}`、fixture `workdir\三期\fixtures\terrae_dev_env`（FIXTURE_MANIFEST.json）
- 步5：`行为重跑\三期\summary.json`（cross 13/13）
- 步6：`圆桌专项\三期\{升降档-transcript.md,升降档-judge-A.json,升降档-judge-B.json,升降档-cross.json}`、`T3_批次A.json`（首跑 FAIL）／`T3_批次A_rerun1.json`（修复后 PASS）、`T5\R1-*`、`T5\R2-*`、`T5\R3-*`（首轮 FAIL 件＋rerun1 三件套）、`step6-summary-final.json`
- 步7：`KB复核\三期\批次B_逐锚复核.json`、`行为重跑\三期\批次B\{mydei-transcript.md,mydei-scores.json,mydei-cross.json,march7th-transcript-canonical.md,march7th-scores-canonical.json,march7th-cross.json,blind_inputs\*.json,batchB-pilot.json}`

### 回滚与恢复

- 三期最终生产相对三期步0基线只变3文件：Terrae题卡＋万敌/三月七persona；44文件双树一致。
- ROLLBACK.ps1在两棵disposable最终modified副本上恢复原始44件baseline并双validate PASS；生产未回滚，保留三期修改。

## 四期执行

- 执行方：主会话（用户授权「你直接动手改吧」，2026-09-04）；三轨顺序执行：文书轨→探针→门重建复测。生产树四期**零改动**（44 文件对 `00_基线\四期\baseline_skills` 全等，两树互等；validate 双树 PASS）。

### 步0

- 两树 44 文件指纹＝三期终态 final_modified_skills 全等；四期基线快照 `00_基线\四期\baseline_skills`＋`prod_sha256_改前.json`。

### 步1 文书轨（零 SHA）

- 验收单三期节：补「步7 内核区相对 D1 成稿的漂移申报」「三期偏离申报」「三期失败轨迹与补丁索引」三段，步7 加四期审核注（`workdir\四期\step1_docs.py`）。
- `KB复核\三期\批次B_逐锚复核.json`：M-D04／ME-D01 两条 status 改 `substring_after_nested_bracket_normalization` 并注非码点逐字（CRIT3-03 方案①）。
- 遗留清单：L-26 改写（门件不具裁决资格、增益未判定）、L-27 补、新立 L-28（两卡内核区保留态）／L-29（terrae 题面耦合）／L-30（内核区漂移申报），恢复点改三择。
- 停止报告三期步7 追记三择；三件 canonical-merge 追加 `canonical_transcript_sha256_scope`；换题清单_三期 补 T05 难度申报＋T01 耦合申报；VERIFICATION [PHASE3_ARTIFACTS] 扩列 12 指针。

### 步2 探针（带／不带内核区审计词密度）

- 4 题（MARCH7TH-01/02/03、MYDEI-05）× 2 变体 × 3 次＝24 份隔离生成（同 SKILL/common；persona 二选一）。审计词/千字 rel Δ：M01 −11.8%、M02 −15.8%、M03 +0.8%、MYDEI-05 −23.1%；≥+20% 题数 0/4 → **探针不成立**；白名单③不触发，persona 零改动；D3-4「内核区推高工作场审计腔」假设关闭。凭证 `workdir\四期\probe\{manifest.json,responses_index.json,probe_result.json,PROBE_CLOSEOUT.json}`。
- 通道注记：宿主子代理 .output 文件本批为 0 字节，24 份回复以完成通知正文逐字落盘（`probe\_responses_from_notifications.py`），如实登记。

### 步3–4 门重建与复测

- 规格 G1–G6 逐字执行（题源 D2 T1 P1/P2/P3；陪聊场起题；old 侧＝三期基线 persona 新鲜生成、new 侧＝生产 persona；sealed_map 先封后判 SHA `c7b6228fb83aa9a4…`／R2 `d53e9dbb5a87a448…`；每对双盲评员＋剥贴片（泄漏扫描 0）；配对评员；功效核算 α≈16.4%、每对 +1 检出 ≈68%）。
- R1：合计 new 22.0 vs old 22.0（Δ0）；万敌 10 vs 12（−2）、三月七 12 vs 10（+2）；双评同向 4/6；配对 2/2。**FAIL**。
- R2（G6 复跑一，换序、新封存、12 份全部新鲜重生成）：合计 23.5 vs 21.5（**Δ+2.0**）；万敌 11 vs 10（+1）、三月七 12.5 vs 11.5（+1）；两卡各不劣；双评同向 6/6。未达 +3 边际 → **FAIL**。两轮非同因。
- 裁决：门 FAIL（弱正增益未达预设边际）；批次C `NOT_RUN_DUE_GATE_STOP`；T1–T5 最终全量 `NOT_RUN`；批次D 未授权。停止证据：`失败轨迹\四期_步骤4_门复测两轮未达边际停止.md`。
- 定性发现：双评两轮一致——万敌 new 侧扣分集中于陪聊中「把 persona 引文当原话念出并带括注出处」（引证腔）；old 侧同样出现，根因＝common 陪聊直引条款在台词层的礼仪缺口（非内核区特有）。已入恢复点 (B)。

### 偏离申报

- 白名单①（门重建）：按规格执行，两轮；②（文书六件）：完成；③（条件性 persona 最小修补）：探针不成立→**未触发，零改动**；④（批次C）：门未过→未进入。白名单外生产树差异：0。
- 通道：全部生成与判卷由本会话隔离子代理承担；判卷者不见卡名／版本／sealed_map；生成者与判卷者为不同子代理实例。

### 回滚与恢复

- ROLLBACK.ps1（20EBF65B 在位）于 `workdir\四期\rollback_test\{codex,claude}` 两棵 disposable 副本亲跑：exit 0，双副本 44/44 恢复至一期改前 baseline（True/True），生产零触碰（True）；`00_基线\四期\rollback_终验.json`。
- 两树 `validate.py --wave all`：codex exit 0、claude exit 0（`00_基线\四期\validate_终态.json`）。

### 四期失败轨迹与凭证索引

- `失败轨迹\四期_步骤4_门复测两轮未达边际停止.md`
- `workdir\四期\gate\{ROUND1_CLOSEOUT.json,GATE_VERDICT.json,gate_result.json,gate_manifest.json,sealed_map.json,sealed_map.sha256,power_analysis.json,strip_list.json,judge_templates.json,responses_index.json}`、`gate\judging\{judge_results_raw.json,pairs_index.json,*__A.txt,*__B.txt}`、`gate\responses\*.response.md`（12）
- `workdir\四期\gate\round2\{sealed_map.json,sealed_map.sha256,order.json,gate_result_r2.json,responses_index.json}`、`round2\judging\judge_results_raw.json`、`round2\responses\*.response.md`（12）
- `workdir\四期\probe\*`（manifest／24 prompt／24 response／responses_index／probe_result／PROBE_CLOSEOUT）
- `workdir\四期\{step0_baseline.py,step1_docs.py,build_probe_bundles.py,analyze_probe.py,step7_closeout.py}`、`gate\{build_gate_bundles.py,prepare_judging.py,build_judge_prompts.py,adjudicate.py,round2\adjudicate_r2.py}`

### 当前恢复点

见停止报告三择 (A)/(B)/(C)；裁量权归用户与审核方。本批建议：(B) 一句合同修补＋一次 65 全量＋复测——因两轮双评把同一缺陷归到合同层而非卡层，且该修补同时闭合 L-23（C-1 指代）。

## 五期执行

- 执行方：主会话（用户「你自主直接判断，修好，全做完」授权，2026-09-04）；路径＝四期恢复点 (B)。前会话在 65 题落盘阶段因上下文断链停摆，本会话从其保存的子代理转录（`~/.claude/projects/D----/ccfeda1a-…/subagents/`）逐字恢复 77 份产出（32 份与已落盘副本逐字一致、45 份新补），未重生成。

### 步1 合同修补（唯一生产变更）

- `amphoreus/references/common.md` 2914e3b9 → **b5b676de**（111 行不变），三处：陪聊直引礼仪（L-31）、圆桌台词内直引同向、「本表词项」指代收紧（L-23）。`diff/五期_common直引礼仪.diff`；两树同步；validate codex/claude 均 exit 0。其余 43 文件与三期终态全等，两树互等（`workdir/五期/tree_check.json`）。

### 步2 65 全量回归（common 变更铁律）

- 表演包＝新 common＋relations(陪聊题)＋SKILL＋persona＋冻结题面输入（评员不见卡文；表演体不见 rubric/期望）。结果 **769/780、65/65 PASS、硬失败 0**（三期批次A 基线 767/780）。分卡：aglaea 57, anaxa 59, castorice 60, cerydra 60, cipher 59, cyrene 60, hyacine 60, hysilens 60, march7th 60, mydei 59, phainon 59, terrae 57, tribbie 59。
- 判卷制：单评为准；三题输入漂移重演题与一题争议题取多评逐项中位（`scores_final65.json` 保留全部评员原判）。
- 输入漂移申报（L-35）：`workdir/blind_inputs/hysilens.json、terrae.json` 中 HYSILENS-04／TERRAE-01／TERRAE-05 为三期题面校准前的旧输入，首轮被评员判「缺材料」；已从 `evals/*.md` 重同步（旧件存 `regression/blind_inputs_pre_resync/`），三题按冻结题面重演＋双评：12／12／11.5。二期、三期转录复核显示当时使用的是正确题面，漂移只存在于该辅助文件。
- MYDEI-04：首评 11（F1=1，认为移交物未成包）→ 追加两名独立评员均 12（冷启动无前文、拒绝虚构属正当）→ 三评中位 12。

### 步3 门复测＋合同 A/B

- L-31 词表命中（12 份/轮）：四期 R1/R2 自报出处 21／25、括注 9／13 → 五期 R1/R2 **0／0、0／0**。
- 合同 A/B（同生产 persona，仅 common 不同；四期 R2 new 侧 vs 五期 R1 new 侧；剥贴片＋封存 `35d6318b…`＋双盲评）：新 common **24.0 vs 17.5（Δ+6.5/30）**，6/6 对、12/12 评员同向，10/12 条理由点名旧侧引证腔。
- 门（G1–G6 原规格）：R1 Δ+0.0（22.5 vs 22.5；双评 4/6；配对 2/2）；R2 Δ+2.0（22.5 vs 20.5；万敌 10.5/10.5、三月七 12.0/10.0；双评 5/6；配对 2/2）。两轮 **FAIL**（未达 +3）。四轮合并 24 对 Σ+4.0/120，p＝0.1796；三月七 +5、万敌 −1 → 内核区增益卡间不对称（L-34）。
- 裁决：批次C `NOT_RUN_DUE_GATE_STOP`；T1–T5 最终全量 `NOT_RUN`；批次D 未授权；两卡内核区维持 L-28 保留态。停止证据：`失败轨迹\五期_步骤3_门复测两轮未达边际停止.md`。

### 偏离申报

- 白名单：common 一句修补（四期恢复点 B 所列）＋随行 L-23 收紧＝本期唯一生产变更；persona／SKILL／evals／rubric／validate 零改动。
- 通道：本期生成与判卷由本会话 Workflow 隔离子代理承担（判卷者不见卡名／版本／sealed_map；生成者与判卷者为不同实例；结构化输出直接落盘）；首轮 65 题表演体为前会话子代理产出，经保存转录逐字恢复。

### 回滚与恢复

- ROLLBACK.ps1（20EBF65B）于 `workdir\五期\rollback_test\codex、claude` 两棵 disposable 副本亲跑（Windows PowerShell）：exit 0，双副本 44/44 恢复至一期改前 baseline（True/True），生产零触碰（True）；`00_基线\五期\rollback_终验.json`。
- 两树 `validate.py --wave all`：codex exit 0、claude exit 0（`00_基线\五期\validate_终态.json`）。快照：`00_基线\五期\baseline_skills(=三期终态)、final_modified_skills、prod_sha256_改后.json`。

### 当前恢复点

见停止报告两择 (i)/(ii)；裁量权归用户与审核方。本批建议：(ii) 维持现状——用户原始诉求（闲聊是本人、工作才上方法论）在合同层已闭合并有 A/B 证据；内核区扩卡假设四轮未证实，不宜再投。

待核：交 fable5 审核，勾选权在审核方与用户
